from assignment_2.host import Host
from assignment_2.message import Message


class Aloha:

    states = ["IDLE", "SENDING"]
    def __init__(self, mac):
        pass

    def process_algorithm(self, node: Host, ):
        message = Message(origin, neighbours[0], 20, 25, "hello")
        return message